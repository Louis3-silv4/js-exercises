/*
1. Crie uma função que recebe dois parametros e retorna a soma deles. Chame essa função e atribua o resultado da execução dela numa variável
e, por fim, imprima essa variável.
*/

function soma(a,b) {
    return a + b 
}

const resultado = soma(2, 3)

console.log(resultado)

const somar = (a, b) => {
  return a + b
}

console.log(somar(5, 5))